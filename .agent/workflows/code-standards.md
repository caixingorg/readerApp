# 代码规范检查清单

> 通用的代码质量参考指引，补充项目自动化检查工具

## 规范优先级

**严格遵循以下优先级顺序**：

1. **项目配置文件（最高优先级）**
   - `.eslintrc.js` / `eslint.config.js` - ESLint 规则
   - `tsconfig.json` - TypeScript 配置
   - `.prettierrc` - 代码格式化规则
   - 项目 `package.json` 中的 lint scripts

2. **项目级规则文件**
   - `agent-rules.md` / `vue-rules.md` / `react-rules.md` / `nextjs-rules.md`
   - 项目特定的技术栈约束和架构规范

3. **本文档（参考指引）**
   - 补充自动化工具检查不到的最佳实践
   - 提供代码审查的参考维度
   - 作为新项目建立规范的模板

## ⚠️ 重要原则

- **遵循项目现有规范**：不要因为本文档而违反项目配置
- **配置即真理**：如果 ESLint 允许 `any`，项目内就可以用
- **团队共识优先**：团队约定的规范高于个人偏好
- **渐进式改进**：老项目逐步提升，不要一次性大改

## 使用场景

本清单在以下场景作为**补充参考**：
- ✅ 提交代码前自查（结合项目 lint 工具）
- ✅ 代码审查时参考（那些工具检查不到的地方）
- ✅ 新项目建立规范时参考
- ✅ 团队规范讨论时参考

## 检查层级

### ⚡ 快速检查（2 分钟）
提交代码前的基本检查

### 🔍 标准检查（5-10 分钟）
PR 提交前的完整检查

### 🎯 深度检查（15-20 分钟）
重要功能或定期审计

---

## ⚡ 快速检查清单

### 第一步：运行项目配置的检查工具（必须通过）

```bash
# 查看项目 package.json 中配置的 scripts
# 运行项目定义的检查命令（可能不同）

# 常见命令（以项目实际配置为准）
npm run type-check  # 或 tsc --noEmit
npm run lint        # 或 eslint . --ext .ts,.tsx
npm run lint:fix    # 自动修复（如有配置）
npm run test        # 单元测试
npm run build       # 构建检查
```

**检查要点**：
- [ ] 无 TypeScript 类型错误（遵循项目 `tsconfig.json`）
- [ ] 无 ESLint 警告或错误（遵循项目 `.eslintrc`）
- [ ] 代码格式符合 Prettier（如项目使用）
- [ ] 测试全部通过
- [ ] 构建成功

### 代码清理
- [ ] 无 `console.log` / `debugger` 遗留
- [ ] 无注释掉的代码块
- [ ] 无临时测试代码
- [ ] 无 `TODO` 注释（除非有计划）

### Git 提交
- [ ] 提交信息符合规范（Conventional Commits）
- [ ] 无敏感信息（密钥、密码、Token）
- [ ] `.gitignore` 配置正确

---

## 🔍 标准检查清单

### 1. TypeScript / JavaScript

#### 类型安全
- [ ] **禁止 `any`**：除非有充分理由并添加注释说明
- [ ] **完整类型定义**：函数参数、返回值、组件 Props 都有类型
- [ ] **避免类型断言**：少用 `as`，用类型守卫代替
- [ ] **接口优于类型**：优先使用 `interface`（React/Vue Props）

```typescript
// ❌ 错误
function process(data: any) { ... }

// ✅ 正确
interface ProcessData {
  id: string;
  value: number;
}
function process(data: ProcessData): Result { ... }
```

#### 命名规范
- [ ] **变量/函数**：驼峰命名 `getUserData`
- [ ] **组件**：大驼峰 `UserProfile.tsx`
- [ ] **常量**：大写下划线 `MAX_RETRY_COUNT`
- [ ] **私有属性**：下划线前缀 `_internalState`（可选）
- [ ] **布尔值**：`is/has/can` 前缀 `isLoading`, `hasPermission`

#### 代码质量
- [ ] **函数长度**：单个函数 ≤ 50 行
- [ ] **文件长度**：单个文件 ≤ 200 行（Vue）/ 150 行（React）
- [ ] **参数数量**：函数参数 ≤ 4 个，超出用对象
- [ ] **嵌套深度**：if/loop 嵌套 ≤ 3 层
- [ ] **圈复杂度**：单个函数 ≤ 10

```typescript
// ❌ 嵌套过深
if (user) {
  if (user.role) {
    if (user.role === 'admin') {
      // ...
    }
  }
}

// ✅ 卫语句
if (!user) return;
if (!user.role) return;
if (user.role !== 'admin') return;
// ...
```

### 2. React 特定规范

#### Hooks 规则
- [ ] **Hooks 顺序**：始终在组件顶层调用
- [ ] **依赖数组**：`useEffect`/`useMemo`/`useCallback` 依赖完整
- [ ] **自定义 Hook**：以 `use` 开头
- [ ] **避免过度优化**：非性能瓶颈不用 `useMemo`/`useCallback`

```typescript
// ❌ 错误
if (condition) {
  useEffect(() => { ... }); // Hooks 不能在条件中
}

// ✅ 正确
useEffect(() => {
  if (condition) { ... }
}, [condition]);
```

#### 组件设计
- [ ] **单一职责**：一个组件只做一件事
- [ ] **Props 类型化**：完整的 Props interface
- [ ] **默认值**：必要的 Props 提供默认值
- [ ] **解构 Props**：函数组件参数解构

```typescript
// ✅ 正确
interface ButtonProps {
  text: string;
  variant?: 'primary' | 'secondary';
  onClick: () => void;
}

function Button({ text, variant = 'primary', onClick }: ButtonProps) {
  return <button onClick={onClick}>{text}</button>;
}
```

#### 性能
- [ ] **避免内联函数**：渲染中创建函数/对象
- [ ] **列表 key**：使用稳定唯一的 key
- [ ] **条件渲染**：优先使用 `&&` 而非三元
- [ ] **大列表虚拟化**：>100 项使用虚拟滚动

### 3. Vue 特定规范

#### 组件规范
- [ ] **Composition API**：新组件使用 `<script setup>`
- [ ] **Props 验证**：完整的 Props 类型定义
- [ ] **Emits 声明**：使用 `defineEmits` 显式声明
- [ ] **单文件组件顺序**：`<script>` → `<template>` → `<style>`

```vue
<!-- ✅ 正确 -->
<script setup lang="ts">
interface Props {
  title: string;
  count?: number;
}

const props = withDefaults(defineProps<Props>(), {
  count: 0,
});

const emit = defineEmits<{
  update: [value: number];
}>();
</script>
```

#### 响应式规范
- [ ] **ref vs reactive**：基本类型用 `ref`，对象用 `reactive`
- [ ] **解构丢失响应**：使用 `toRefs` 解构
- [ ] **computed**：派生状态使用 `computed`
- [ ] **watch**：明确 `immediate`/`deep` 选项

#### 模板规范
- [ ] **v-for key**：始终提供 key
- [ ] **v-if vs v-show**：频繁切换用 `v-show`
- [ ] **事件修饰符**：合理使用 `.stop`/`.prevent`
- [ ] **指令简写**：`v-bind` → `:`, `v-on` → `@`

### 4. Next.js 特定规范

#### 组件分类
- [ ] **Server Component**：默认使用（无 `'use client'`）
- [ ] **Client Component**：需要交互时添加 `'use client'`
- [ ] **边界明确**：Client Component 尽可能小

#### 数据获取
- [ ] **Server Component 获取**：优先在服务端获取数据
- [ ] **缓存策略**：明确 `cache` 和 `revalidate`
- [ ] **并行请求**：使用 `Promise.all` 并行
- [ ] **错误处理**：使用 `error.tsx` 边界

#### 优化
- [ ] **next/image**：所有图片使用
- [ ] **next/font**：字体优化
- [ ] **metadata**：使用 `generateMetadata` 生成 SEO
- [ ] **loading.tsx**：路由添加加载态

### 5. 样式规范

#### CSS 组织
- [ ] **CSS Modules**：避免全局样式污染
- [ ] **命名规范**：BEM 或语义化命名
- [ ] **嵌套层级**：≤ 3 层
- [ ] **单位统一**：使用 `rem`/`em` 而非 `px`

#### Tailwind / Atomic CSS
- [ ] **类名顺序**：布局 → 间距 → 尺寸 → 外观
- [ ] **自定义类**：复杂组合提取为组件
- [ ] **响应式**：移动优先设计

```tsx
// ✅ 类名顺序清晰
<div className="flex items-center gap-4 p-4 bg-white rounded-lg shadow">
```

### 6. 安全规范

#### 输入验证
- [ ] **用户输入验证**：使用 Zod/Yup Schema
- [ ] **XSS 防护**：不直接渲染用户 HTML
- [ ] **SQL 注入**：使用参数化查询（后端）
- [ ] **CSRF 防护**：API 使用 Token

```typescript
// ✅ 使用 Zod 验证
import { z } from 'zod';

const UserSchema = z.object({
  email: z.string().email(),
  age: z.number().min(0).max(120),
});

const result = UserSchema.safeParse(input);
if (!result.success) {
  // 处理验证错误
}
```

#### 敏感数据
- [ ] **无硬编码密钥**：使用环境变量
- [ ] **环境变量安全**：`.env` 文件在 `.gitignore`
- [ ] **前端存储**：敏感数据用 `secure-store` 或 `keychain`
- [ ] **日志安全**：不打印敏感信息

### 7. 性能规范

#### 打包优化
- [ ] **代码分割**：路由级别懒加载
- [ ] **Tree Shaking**：只导入需要的模块
- [ ] **依赖审查**：无未使用的依赖
- [ ] **打包体积**：单个 chunk ≤ 250KB (gzipped)

#### 运行时优化
- [ ] **图片优化**：使用 WebP/AVIF，懒加载
- [ ] **长列表**：虚拟滚动
- [ ] **防抖节流**：高频事件处理
- [ ] **内存管理**：清理监听器/定时器/订阅

```typescript
// ✅ 清理副作用
useEffect(() => {
  const timer = setInterval(() => {}, 1000);
  window.addEventListener('resize', handler);
  
  return () => {
    clearInterval(timer);
    window.removeEventListener('resize', handler);
  };
}, []);
```

### 8. 测试规范

#### 测试覆盖
- [ ] **关键逻辑**：utils/hooks 必须有单元测试
- [ ] **覆盖率**：核心模块 ≥ 80%
- [ ] **测试命名**：`it('should ... when ...')`
- [ ] **测试独立**：不依赖执行顺序

```typescript
// ✅ 测试命名清晰
describe('useAuth', () => {
  it('should return user data when logged in', () => {
    // ...
  });
  
  it('should return null when not logged in', () => {
    // ...
  });
});
```

### 9. 文档与注释

#### 注释规范
- [ ] **复杂逻辑**：添加 Why 注释（为什么这样写）
- [ ] **Hack/Workaround**：必须注释原因
- [ ] **正则表达式**：解释匹配逻辑
- [ ] **算法**：说明时间/空间复杂度

```typescript
// ✅ 好的注释
// Why: Safari 不支持 lookbehind，使用替代方案
const regex = /(?:^|\s)@(\w+)/g;

// Why: 避免 React 18 的 double render 导致重复请求
const hasRequestedRef = useRef(false);
```

#### 避免的注释
- [ ] **显而易见的注释**：`// Set name to value`
- [ ] **注释掉的代码**：删除而不是注释
- [ ] **过时注释**：修改代码时同步更新注释

---

## 🎯 深度检查清单

### 架构与设计

#### 代码组织
- [ ] **模块化**：功能内聚，耦合松散
- [ ] **依赖方向**：低层不依赖高层
- [ ] **循环依赖**：无循环引用
- [ ] **文件结构**：遵循项目规范

#### 设计原则
- [ ] **DRY**：无重复代码（≥3 次重复必须抽离）
- [ ] **KISS**：保持简单，避免过度设计
- [ ] **YAGNI**：不实现暂不需要的功能
- [ ] **单一职责**：每个模块/函数只做一件事

#### 可维护性
- [ ] **易读性**：新人能快速理解
- [ ] **可测试性**：逻辑易于单元测试
- [ ] **可扩展性**：新需求无需大改
- [ ] **向后兼容**：不破坏现有 API

### 错误处理

- [ ] **边界情况**：null/undefined/空数组/空字符串
- [ ] **错误捕获**：try-catch 或 Error Boundary
- [ ] **错误提示**：用户友好的错误信息
- [ ] **降级方案**：关键功能有 fallback

```typescript
// ✅ 完善的错误处理
try {
  const data = await fetchData();
  if (!data) {
    throw new Error('No data received');
  }
  return processData(data);
} catch (error) {
  logger.error('Failed to fetch data', error);
  toast.error('加载失败，请稍后重试');
  return fallbackData;
}
```

### 可访问性（A11y）

- [ ] **语义化 HTML**：使用正确的标签
- [ ] **键盘导航**：可用 Tab/Enter 操作
- [ ] **ARIA 标签**：必要时添加 aria-*
- [ ] **对比度**：文字与背景对比度 ≥ 4.5:1
- [ ] **焦点可见**：focus 状态明显

### 国际化（i18n）

- [ ] **硬编码文案**：提取到语言包
- [ ] **日期格式**：使用 Intl.DateTimeFormat
- [ ] **数字格式**：使用 Intl.NumberFormat
- [ ] **复数处理**：正确处理单复数

---

## 项目特定规范

### 根据项目类型补充

**React Native 项目**：参考 [agent-rules.md](agent-rules.md)
**Vue 项目**：参考 [vue-rules.md](vue-rules.md)
**Next.js 项目**：参考 [nextjs-rules.md](nextjs-rules.md)

---

## 自动化检查配置

### ESLint 配置示例

```javascript
// .eslintrc.js
module.exports = {
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react-hooks/recommended',
  ],
  rules: {
    '@typescript-eslint/no-explicit-any': 'error',
    '@typescript-eslint/explicit-function-return-type': 'warn',
    'react-hooks/rules-of-hooks': 'error',
    'react-hooks/exhaustive-deps': 'warn',
    'no-console': 'warn',
    'max-lines': ['warn', 200],
    'max-depth': ['warn', 3],
    'complexity': ['warn', 10],
  },
};
```

### Pre-commit Hook

```bash
# .husky/pre-commit
#!/bin/sh
npm run type-check
npm run lint
npm run test -- --watchAll=false
```

---

## 快速指令

- **/check-quick**: 执行快速检查
- **/check-full**: 执行标准检查
- **/check-deep**: 执行深度检查
- **/check-fix**: 自动修复可修复的问题

---

## 检查清单打分

| 检查项 | 权重 | 通过标准 |
|--------|------|----------|
| 自动化检查 | 30% | 全部通过 |
| 代码质量 | 30% | ≥ 90% 符合 |
| 性能规范 | 20% | 无明显问题 |
| 安全规范 | 15% | 全部符合 |
| 文档注释 | 5% | 关键部分完整 |

**合格线**：≥ 80 分
